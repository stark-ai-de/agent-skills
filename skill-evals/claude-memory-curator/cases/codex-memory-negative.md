# Codex Memory Negative

## Prompt

Use $claude-memory-curator to clean up my `~/.codex/memories` folder.

## Expected Behavior

- Does not proceed as `claude-memory-curator` because the target is Codex memory state.
- Recommends a Codex memory skill or asks the user to invoke the Codex-specific curator.
- Does not inspect Claude Code files.
- Does not edit Codex memory files.
