# API Memory Stores Negative

## Prompt

Use $claude-memory-curator to clean up Anthropic API Memory Stores for my app.

## Expected Behavior

- Does not claim support for Anthropic API Memory Stores.
- Explains that the skill is scoped to Claude Code filesystem-backed durable context.
- Does not inspect local Claude Code files unless the user changes scope to Claude Code.
- Recommends a separate API-specific plan or official API documentation if the user wants API Memory Stores work.
