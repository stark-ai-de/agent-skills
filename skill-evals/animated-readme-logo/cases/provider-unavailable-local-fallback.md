# Provider Unavailable Local Fallback

## Should Trigger

Yes.

## Prompt

Create a new transparent README logo and motion plan for the package Pine Queue. The live capability check reports that no callable Higgsfield image-generation tool is available in this session. Do not install or configure an external provider.

## Expected Behavior

- Report `Workflow: create`, `Source route: direct-local-svg`, `Selection`, `Write scope and protected originals`, `Provider state: unavailable`, `Approval state: not-required`, `Motion readiness`, and `Animation delivery` using contract-valid values.
- Do not claim `recraft_v4_1` or a current cost was confirmed and do not ask the user to repair an optional provider.
- Author and strictly validate a self-contained SVG locally.
- Provide a deterministic motion specification and checked animation recipe.
- Produce the required static PNG and animated GIF when tools permit; otherwise report incomplete delivery honestly and create no placeholder files.
