# Existing remote setup

## Should Trigger

Yes.

## Prompt

Configure Hetzner on my existing LiteLLM gateway. Use the admin key from my protected file, add a new model alias and test it with my separate inference key. You may execute these steps.

## Expected behavior

- Select remote/autonomous without a repeated global approval question.
- Inspect management and inference URLs, compatible API and model ownership.
- Use a discovered provider model and protected sources; never reuse admin access for inference clients.
- Apply the current plan, read back, then separately verify inference.
