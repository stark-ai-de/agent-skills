# Text-only behavior evaluation

An implementation-independent reviewer authored and scored six concise responses from the skill, references and target/mode rubric on 2026-09-21. This was a known-expectations simulation, not a blind activation benchmark or an executed agent workflow. No simulated action read a secret, contacted an endpoint, installed software or changed state.

| Case                      | Observed response                                                                                                                                                                                                                                           | Applicable text score |
| ------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------- |
| remote-autonomous         | Selects remote/autonomous from explicit intent; asks missing management/inference URLs, protected sources, alias and current model; separates readback, inference and receipt-owned rollback. No unperformed execution credit.                              | 16/16                 |
| manual-local-no-key       | Gives Windows venv, exact LiteLLM/FastAPI/Starlette install pins and pip check, private discovery/start snippets, separate local gateway key, inference check and Ctrl+C. Does not read keys. Referenced snippets reduce standalone usability by one point. | 19/20                 |
| manual-remote-ui          | Explains Models + Endpoints / Add Model, OpenAI-compatible provider, exact provider base and model, private key entry and LiteLLM Params bridge flag; prerequisites and version-specific controls remain explicit.                                          | 18/18                 |
| remote-gitops-owner       | Returns an update for the existing model_list entry with a remote environment reference; owning GitOps operator applies and reverts it. No UI override or deployment claim.                                                                                 | 18/18                 |
| remote-timeout-recovery   | Retains original plan/journal; reconciles exact model ID, marker and non-secret configuration. Does not repeat POST or treat an alias as ownership. Execution remains unrun.                                                                                | 16/16                 |
| generic-provider-negative | Rejects the Hetzner-specific workflow for another vendor, asks for generic provider/target/mode, and performs no setup.                                                                                                                                     | 8/8                   |

All authored responses pass the text threshold without an observed hard-gate violation. Unselected criteria are N/A, never passes. The prompts are the named files under `../cases/`; the complete scoring rules are in `../rubric.md`.

This exercise provides response-coverage evidence only. It proves no native host behavior, credential protection, HTTP contract, UI availability, dependency installation, live inference, tool continuation or coding-client compatibility, and no measured improvement against a baseline. Runtime, live and hosted evidence are recorded separately.
