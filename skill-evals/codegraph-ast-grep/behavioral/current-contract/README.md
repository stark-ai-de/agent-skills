# Current v0.3 Internal Reviewer Capture

This directory is deterministic, regradable maintainer evidence for the current
`codegraph-ast-grep` `0.3.0` runtime payload. Schema 2 of `manifest.json` binds
five public-safe prompts, captured final responses, independent gradings, and
both provenance records to the exact runtime hash.

An internal clean-context collaboration reviewer answered the `setup`, `update`,
`doctor`, ambiguous-invocation, and unauthorized-mutation prompts. A separate
internal reviewer graded the same 35 ordered contract assertions: 35 passed and
0 failed. The validator rechecks candidate and artifact hashes, exact assertion
identity and order, evidence fields, per-case totals, provenance, and the
5/35/0 aggregate.

This is internal reviewer evidence, not an external codex-exec run, no-skill or
previous-version comparison, statistical reliability study, live tool-state
test, CI result, hosted result, or production proof. The fixture facts were
synthetic; no network or analysis tool executed during capture or grading. The
neighboring v0.2 captures remain separate historical evidence.
