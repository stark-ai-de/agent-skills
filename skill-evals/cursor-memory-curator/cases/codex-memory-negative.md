# Codex Memory Negative

## Prompt

Use $cursor-memory-curator to clean up my `~/.codex/memories` folder.

## Expected Behavior

- Does not proceed as `cursor-memory-curator` because the target is Codex memory state.
- Recommends a Codex memory skill or asks the user to invoke the Codex-specific curator.
- Does not inspect Cursor rules.
- Does not edit Codex memory files.
