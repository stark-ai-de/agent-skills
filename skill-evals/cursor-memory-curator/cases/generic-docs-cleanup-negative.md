# Generic Docs Cleanup Negative

## Prompt

Please clean up the README wording and make the project docs easier to scan.

## Expected Behavior

- Does not trigger `cursor-memory-curator`.
- Proceeds as ordinary repo documentation work because the prompt does not mention Cursor rules, persistent Cursor context, User Rules, Team Rules, `.cursorrules`, or memory-bank artifacts.
- Does not inspect `.cursor/rules` by default.
