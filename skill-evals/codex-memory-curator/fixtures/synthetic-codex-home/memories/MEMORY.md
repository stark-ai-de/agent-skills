# Synthetic Codex Memory

- User prefers pnpm when the current repository supports it.
- Run `pnpm turbo test` in the old platform repository before every final answer.
- Never edit files under `packages/generated-client`.
- Current migration branch is `fix/temporary-api-retry`.
- The old credential-shaped test string is `synthetic-secret-value-redacted`.
- Long-running memory cleanup should produce a review report before editing.
