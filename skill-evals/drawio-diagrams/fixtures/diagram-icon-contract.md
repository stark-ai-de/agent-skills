# Diagram Icon Contract

This synthetic contract applies only when a diagram request explicitly references it.

- Admin UI: labelled user-console semantic icon.
- API: labelled API-gateway semantic icon.
- Job Queue: labelled native queue symbol.
- Worker: no repository mapping; choose and report one labelled semantic fallback.
- Keep all icon chips neutral and consistent.
- Do not fetch remote assets for these four components.
