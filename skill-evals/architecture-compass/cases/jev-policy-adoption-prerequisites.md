# Jev Policy Adoption and Independent Prerequisites

## Should Trigger

Yes.

## Prompt

Use Architecture Compass setup to adopt a repository rule requiring Jev capability advice. This established repository allocates ADR-012 next and has no conflicting decision. I authorize writing its governance and mapping only. The intended host is Codex CLI on Linux/WSL. Jev is installed and a policy fragment is rendered, but the host owner has not applied or trusted it. No TypeSafe processing is authorized, the only capability list came from another session, and no native qualification receipt exists. Preserve our accepted decisions and show the operational gaps.

## Deterministic Assertions

- contains: Selected workflow: setup
- contains: AC-ADR-065
- contains: ADR-012
- contains: installed
- contains: configured
- contains: active/trusted
- contains: processing authority
- contains: current inventory
- contains: qualification
- contains: unmet obligation
- contains: native selection
- not_contains: TypeSafe processing authorized by adoption
- not_contains: host qualified by rendered fragment

## Expected Behavior

- Expose the five workflows, select `setup/recommended`, and preserve the exact seven-decision foundation without applying it by default to this established repository.
- Adopt AC-ADR-065 through repository-native ADR-012 and a provider mapping within the authorized governance scope; do not copy the provider ID into the local sequence.
- Record Codex CLI Linux/WSL as the intended host scope and distinguish installation from a rendered candidate, applied configuration, effective activation/trust, processing authority, current eligibility, and practical qualification.
- Report the missing activation, trust, scoped processing consent, reliable current inventory, and native evidence as unmet obligations. The previous session's metadata is not current eligibility or a complete authoritative inventory.
- Do not install, change host configuration or credentials, grant trust, send task data, call TypeSafe, or infer authority from setup or adoption. Preserve native selection for otherwise authorized work.
- Explain that later authorized operation uses general/current for eligible skills and tools on new tasks or material task/capability changes, with no repeated advice for unchanged messages, and requires separately observed native qualification.
