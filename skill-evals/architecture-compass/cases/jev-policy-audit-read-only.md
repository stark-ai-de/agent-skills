# Audit Jev Adoption without Calling or Repairing the Provider

## Should Trigger

Yes.

## Prompt

Audit compliance with our accepted local ADR-012 mapping to AC-ADR-065; report only. A global Codex hook serves this adopting repository and another repository that has not adopted the policy. The advisor is installed and the registration is configured. Last week's receipt records a trusted native run on a different CLI version and model/reasoning setting. We have explicit permission for minimal summaries and approved capability metadata for this repository only. The current model-visible capability list omits invocation restrictions and account eligibility, and has no authoritative completeness proof. A newer provider-error receipt records native continuation. Do not change files or host state.

## Deterministic Assertions

- contains: Selected workflow: audit
- contains: strictly read-only
- contains: AC-ADR-065
- contains: processing authority
- contains: current inventory
- contains: stale
- contains: unmet obligation
- contains: native selection
- contains: no provider requests
- not_contains: qualification passed
- not_contains: repair completed

## Expected Behavior

- Expose all five workflows and select audit from clear report-only intent. Inspect only available repository and host evidence; return the report in the conversation without writing an artifact.
- Separate installation, configuration, effective active/trusted state, scoped processing authority, current inventory/restrictions, and practical qualification. A previous trusted run cannot prove the new host/model/reasoning configuration is qualified.
- Mark dependent qualification stale, missing restriction/coverage evidence unresolved, and the provider error as failed advice with native fallback. Do not claim complete inventory from model-visible metadata or absent flags; preserve explicit user choices and validate recommendations before any separately authorized execution.
- Make no provider requests, qualification probes, renderer-to-file writes, hook repairs, trust changes, credential access for a smoke, or catalog/receipt/cache writes. Existing processing authority does not turn audit into a new consultation or qualification workflow.
- Report unmet obligations and the separately authorized follow-up that would establish fresh evidence without blocking unrelated native work. The non-adopting repository remains outside the policy despite the shared global registration.
- Distinguish local fixtures, prior native observations, current qualification, and publication. Avoid repeated advice/status spam for unchanged audit follow-ups, retries after failure, or unsupported claims about other hosts.
