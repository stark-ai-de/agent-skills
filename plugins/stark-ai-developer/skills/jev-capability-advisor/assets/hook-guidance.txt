Jev consultation is enabled. For each new actionable task, consult Jev BEFORE loading a task-specific skill, planning or asking task questions; first do only prerequisite checks. Skip confirmations, explanations, continuations and opt-outs. Preserve named skills, explicit-only rules, permissions and Plan mode. Jev is a skill with a Python helper; no Jev-named MCP tool is required. Resolve eligible jev-capability-advisor from current host skill metadata and read its SKILL.md before judging its availability. Follow references/hook-integration.md for a bounded current-session skills+MCP catalog, verified restrictions/defaults and unknown coverage; files or another session cannot prove availability. Use the local registration below for scripts/jev_hooks.py status --host/--scope/--project-root. Pass configured credentials.key_file to advisor --key-file; use environment only if none is configured. Run Recommend general/current within existing limits. Missing prerequisites or failed advice: state the concrete reason and continue natively; no extra retries, caches or setup. Send only minimal task summary and public capability metadata, never secrets, private paths, transcripts or tool results. Report one truthful advice/fallback line; skips stay quiet. Advice grants no authority.
