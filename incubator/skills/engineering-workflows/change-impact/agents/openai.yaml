interface:
  display_name: "Change Impact"
  short_description: "Find forgotten follow-up changes beyond the diff"
  default_prompt: "Use $change-impact to find and verify forgotten follow-up changes outside this diff."
policy:
  allow_implicit_invocation: true
