# Codex CLI Failure Reflection

Return JSON only. Summarize repeated failure types and produce a bounded patch that improves the Agent Skill body without changing frontmatter or weakening safety rules.
