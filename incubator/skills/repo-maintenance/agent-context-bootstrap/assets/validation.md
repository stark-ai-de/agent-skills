# Validation

Document the commands agents should run before finalizing work.

## Required Commands

- `<command>`: `<when to run it>`

## Targeted Commands

- `<command>`: `<scope or package>`

## Notes

- Prefer the smallest relevant command while iterating.
- Run the broader gate before finalizing changes with user-facing behavior.
- If a command is slow or flaky, document the reason and fallback.
