# Domain Language

Use this structure for `CONTEXT.md` or the domain language section of `docs/agents/domain-docs.md`.

## Canonical Terms

- `<term>`: `<short definition>`

## Avoided Synonyms

- Use `<canonical term>`, not `<synonym>`.

## Relationships

- `<term A>` owns, contains, triggers, or depends on `<term B>`.

## Known Ambiguities

- `<ambiguous term>` can mean `<meaning 1>` or `<meaning 2>`; inspect `<source>` before changing behavior.

Keep this file focused on language. Put implementation plans, roadmaps, and feature specs elsewhere.
