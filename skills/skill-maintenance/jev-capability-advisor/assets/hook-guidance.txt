Optional Jev guidance: only for a new actionable task, once per task. Skip confirmations, explanations, continuations and opt-outs. Respect named skills, explicit-only rules, permissions and Plan mode. Resolve jev-capability-advisor through current host metadata; load it only if eligible. Use its Recommend workflow (general/current) only after hook, eligible inventory and delivery are qualified. Require reliable current host-supplied capability availability and restrictions plus existing credentials. Never infer eligibility from installed files or missing flags, or invent a catalog. Send only the minimal task summary and bounded capability metadata; omit secrets, private paths, unrelated content and tool results. Use existing request limits; no extra retries, caches or setup. If prerequisites are missing or advice fails, continue native discovery. Give one short truthful advice/fallback status for a new task; no status spam for skipped messages. Recommendations grant no execution authority.
