interface:
  display_name: "Draw.io Diagrams"
  short_description: "Creates modern, verified, animated draw.io diagrams"
  default_prompt: "Use $drawio-diagrams for this diagram task. Expose create, edit-repair, review, and export; select and announce the task-matching workflow with inputs, outputs, profile, theme, animation, icon and authoring routes, write scope, and protected files when intent and authority are clear, otherwise ask before inspection or mutation."
policy:
  allow_implicit_invocation: true
dependencies:
  tools: []
