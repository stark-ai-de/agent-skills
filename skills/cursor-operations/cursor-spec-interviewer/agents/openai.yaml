interface:
  display_name: "Cursor Spec Interviewer"
  short_description: "Interviews, verifies, ADR-gates, and saves Cursor-ready specs"
  default_prompt: "Use $cursor-spec-interviewer with the current execution host's planning, structured-question, transition, and plan-exit controls to interview me, source-check the requirements, ADR-gate durable decisions, verify the final scope, then persist the repository-owned spec and emit a Cursor-targeted execution prompt without implementing it."
policy:
  allow_implicit_invocation: true
dependencies:
  tools: []
