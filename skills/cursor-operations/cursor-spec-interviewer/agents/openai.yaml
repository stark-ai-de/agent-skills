interface:
  display_name: "Cursor Spec Interviewer"
  short_description: "Interviews, verifies, ADR-gates, and saves Cursor-ready specs"
  default_prompt: "Use $cursor-spec-interviewer to interview me, source-check the requirements, ADR-gate durable decisions, verify the final scope, and save a Cursor-ready implementation spec."
policy:
  allow_implicit_invocation: true
dependencies:
  tools: []
