interface:
  display_name: "Cursor Memory Curator"
  short_description: "Audits and safely cleans up Cursor rules and persistent context"
  default_prompt: "Use $cursor-memory-curator for my Cursor rules and persistent context. Show all eight workflows with plan-run-cleanup-file first and Recommended; select and announce the intent-bound route when my outcome and authority are clear, otherwise ask me to choose."
policy:
  allow_implicit_invocation: true
dependencies:
  tools: []
