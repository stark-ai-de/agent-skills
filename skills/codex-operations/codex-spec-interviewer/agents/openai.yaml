interface:
  display_name: "Codex Spec Interviewer"
  short_description: "Interviews, verifies, ADR-gates, and persists Codex-ready implementation specs"
  default_prompt: "Interview me first, challenge the requirements against repo evidence and current sources, ADR-gate any architectural decisions, verify the final scope with me, then save a production-ready implementation spec before coding."
policy:
  allow_implicit_invocation: true
dependencies:
  tools: []
