interface:
  display_name: "Codex Spec Interviewer"
  short_description: "Interviews, challenges, ADR-gates, and saves specs"
  default_prompt: "Use $codex-spec-interviewer to interview me, source-check the requirements, ADR-gate durable decisions, and save a Codex-ready implementation spec."
policy:
  allow_implicit_invocation: true
dependencies:
  tools: []
